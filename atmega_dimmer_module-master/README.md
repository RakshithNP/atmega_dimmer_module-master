Dimmer Module and Sub Module

In order to compile the arduino project with modlib after cloning the repository 
`git submodule update --init --recursive`
to clone the modlib library into the project directory.
